<% via_brand = (@sender_name == @brand_title) ? "" : " via #{@brand_title}" %>
<% if @supervisor %>
<%= @sender_name %> shared a secret<%= via_brand %> and copied you as the supervisor.

Note: opening this link counts as one of the views below, the same as it does for the primary recipient.
<% else %>
<%= @sender_name %> has shared a secret with you<%= via_brand %>.
<% end %>
<% if @push.name.present? %>
Label: <%= @push.name %>
<% end %>
Access it securely using this link:
<%= @secret_url %>

Expires in: <%= @push.days_remaining %> <%= @push.days_remaining == 1 ? "day" : "days" %> or <%= @push.views_remaining %> <%= @push.views_remaining == 1 ? "view" : "views" %> (whichever comes first).

This link and all content will be permanently deleted after expiration.

--
Sent via <%= @brand_title %>
